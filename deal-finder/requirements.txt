# The engine runs on the Python 3.11 standard library alone — no install needed.
#
# Optional, only if you extend it to scrape bot-protected sites (Cloudflare, etc.)
# that 403 plain urllib requests:
#   requests
#   playwright            # headless browser for JS-rendered / protected pages
