"""dealfinder: a dependency-free engine that finds and monitors deals for a
specific running-shoe model and computes the landed cost of shipping it to
Israel.

Built around schema.org JSON-LD extraction (what most shoe retailers embed)
rather than brittle per-site CSS selectors.
"""

__version__ = "0.1.0"
